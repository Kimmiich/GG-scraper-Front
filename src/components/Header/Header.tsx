import Link from 'next/link';
import React from 'react';

function Header() {
  return (
    <header>
      <button>Logo</button>
      <button>Go Premium</button>
      <button>Menu</button>
    </header>
  );
}

export default Header;
